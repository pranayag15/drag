
export const mapSelect = (data) => {
  // var mappedFields = "";
  // data.map((field, i) => {
  //   let name = field["name"] ? field["name"] : "field" + (i + 1);
  //   let mode = field["mode"] == "single" ? "" : field["mode"];
  //   let label = field["label"] ? field["label"] : "Enter field" + (i + 1);
  //   let required = field["required"] ? field["required"] : false;
  //   let fieldString = `<Form.Item
  //       name="${name}"
  //       label="${label}"
  //       mode="${mode}"
  //       rules={[
  //           {
  //               required: ${required} 
  //           }
  //       ]}
  //     >
  //       <Select placeholder="Please select ${label}">`;
  //   field["options"].map((option, index) => {
  //     fieldString += `
  //     <Option value="${option["value"]}">${option["option"]}</Option>`;
  //   });
  //   fieldString += `</Select>
  //     </Form.Item>
  //     `;
  //   mappedFields += fieldString;
  // });
  // return mappedFields
  return "hah"
};